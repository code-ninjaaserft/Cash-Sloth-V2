<Window x:Class="CashSloth.App.CustomerDisplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Customer Display" Height="720" Width="1024">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <DataGrid x:Name="CartLinesGrid"
              Grid.Row="0"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              CanUserResizeRows="False"
              IsReadOnly="True"
              HeadersVisibility="Column"
              RowHeaderWidth="0"
              FontSize="20">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Item" Binding="{Binding Name}" Width="*" />
        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="100" />
        <DataGridTextColumn Header="Line Total" Binding="{Binding LineTotalDisplay}" Width="180" />
      </DataGrid.Columns>
    </DataGrid>

    <Grid Grid.Row="1" Margin="0,24,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <StackPanel Grid.Column="0">
        <TextBlock Text="Total" FontSize="18" FontWeight="SemiBold" />
        <TextBlock x:Name="TotalValueText" FontSize="32" FontWeight="Bold" Text="CHF 0.00" />
      </StackPanel>
      <StackPanel Grid.Column="1">
        <TextBlock Text="Given" FontSize="18" FontWeight="SemiBold" />
        <TextBlock x:Name="GivenValueText" FontSize="32" FontWeight="Bold" Text="CHF 0.00" />
      </StackPanel>
      <StackPanel Grid.Column="2">
        <TextBlock Text="Change" FontSize="18" FontWeight="SemiBold" />
        <TextBlock x:Name="ChangeValueText" FontSize="32" FontWeight="Bold" Text="CHF 0.00" />
      </StackPanel>
    </Grid>
  </Grid>
</Window>
